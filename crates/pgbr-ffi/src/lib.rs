//! `pgbr-ffi` placeholder. Filled in by a later phase of the C->Rust migration.
//!
//! This crate exposes a C ABI consumed by the legacy C build during the
//! migration. `unsafe` is intentionally allowed.
